# _printf() - Holberton Manual Page


## Name
_printf()

## Usages

_printf(const char *format, ...);

## What it does
Print the given string (format), in a formatted way by taking into account all flags and extra args given, similar to printf().

## Flags

% preceeds any flag given in the format string given, the list of vaid flags are:
s - string of characters
c - character
% - Print the % symbol
d - Print a given number 
i - Print a given integer

Example s: _printf("string here: %s\n", string_pointer);
Example c: _printf("character here: %c\n", char);
Example %: _printf("100%%\n");
Example d: _printf("Number: %d\n", long int);
Example i: _printf("Int: %i\n", int);

## Returns

The length of the printed string or -1 if an error is hit

